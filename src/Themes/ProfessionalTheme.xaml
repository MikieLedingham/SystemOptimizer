<!-- File: Themes/ProfessionalTheme.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Global Font -->
    <FontFamily x:Key="AppFont">Segoe UI</FontFamily>

    <!-- Main window background color -->
    <SolidColorBrush x:Key="MainWindowBackground" Color="#FFF8F8F8"/>
    <SolidColorBrush x:Key="ButtonBackground" Color="#FF333333"/>
    <SolidColorBrush x:Key="ButtonForeground" Color="#FFF5F5F5"/>

    <!-- Button Style (square, simple, businesslike) -->
    <Style TargetType="Button">
        <Setter Property="FontFamily" Value="{DynamicResource AppFont}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderBrush" Value="#FF666666"/>
        <Setter Property="CornerRadius" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Add more for pro look as you want! -->

</ResourceDictionary>
